import { AdminPagination, formatDate } from "../../../components/admin/AdminPageParts";
import { getAdminReviews } from "../../../server/admin/repository";

export default async function AdminReviewsPage({ searchParams }: { searchParams: Promise<{ page?: string }> }) { const query = await searchParams; const page = Math.max(1, Number(query.page) || 1); const data = await getAdminReviews(page); return <div className="dashboard-page"><header className="dashboard-page-header compact"><div><span className="eyebrow">Güven</span><h1>Yorumlar</h1><p>Kullanıcı değerlendirmelerini ve puanlarını incele.</p></div></header>{data.rows.length ? <div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Yazan</th><th>Değerlendirilen</th><th>Puan</th><th>Yorum</th><th>Teklif</th><th>Tarih</th></tr></thead><tbody>{data.rows.map((review) => <tr key={review.id}><td>{review.reviewer.username}</td><td>{review.reviewedUser.username}</td><td><strong className="admin-rating">{review.rating}/5</strong></td><td>{review.comment ?? "-"}</td><td>{review.offer ? review.offer.id.slice(0, 8) : "-"}</td><td>{formatDate(review.createdAt)}</td></tr>)}</tbody></table></div> : <div className="dashboard-empty-state"><h3>Henüz yorum yok.</h3><p>Değerlendirmeler burada görünecek.</p></div>}<AdminPagination page={data.page} total={data.total} pageSize={data.pageSize} basePath="/admin/yorumlar" /></div>; }
