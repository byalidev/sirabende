import Link from "next/link";
import { notFound } from "next/navigation";
import { AdminStatus, formatDate } from "../../../../components/admin/AdminPageParts";
import { RequestFeatureForm } from "../../../../components/admin/RequestFeatureForm";
import { RequestStatusForm } from "../../../../components/admin/RequestStatusForm";
import { getAdminRequest } from "../../../../server/admin/repository";

export default async function AdminRequestDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const request = await getAdminRequest(id); if (!request) notFound(); const budget = request.maxBudget ? `${request.minBudget?.toString() ?? "0"} - ${request.maxBudget}` : request.minBudget?.toString() ?? "Belirtilmemiş"; return <div className="dashboard-page"><Link className="admin-back-link" href="/admin/talepler">← Taleplere dön</Link><header className="dashboard-page-header compact"><div><span className="eyebrow">Talep detayı</span><h1>{request.title}</h1><p>{request.user.username} · {request.user.email}</p></div><AdminStatus value={request.status} /></header><section className="admin-detail-grid"><div className="dashboard-panel-section"><h2>Talep bilgileri</h2><dl className="admin-detail-list"><div><dt>Açıklama</dt><dd>{request.description ?? "Açıklama eklenmemiş."}</dd></div><div><dt>Kategori</dt><dd>{request.category?.name ?? "Kategorisiz"}</dd></div><div><dt>Konum</dt><dd>{request.city}{request.district ? ` / ${request.district}` : ""}</dd></div><div><dt>Bütçe</dt><dd>{budget} {request.currency}</dd></div><div><dt>Koşul</dt><dd>{request.condition}</dd></div><div><dt>Tarihler</dt><dd>{formatDate(request.createdAt)}{request.expiresAt ? ` · ${formatDate(request.expiresAt)}` : ""}</dd></div></dl></div><div className="dashboard-panel-section"><h2>Durum yönetimi</h2><RequestStatusForm id={request.id} current={request.status} /><h2>Görünürlük yönetimi</h2><RequestFeatureForm id={request.id} values={{ featuredUntil: request.featuredUntil?.toISOString() ?? null, pinnedUntil: request.pinnedUntil?.toISOString() ?? null, urgentUntil: request.urgentUntil?.toISOString() ?? null }} /><p className="admin-note">{request._count.favorites} favori · {request._count.reports} rapor</p></div></section><section className="dashboard-panel-section"><h2>Teklifler</h2>{request.offers.length ? request.offers.map((offer) => <p className="admin-detail-row" key={offer.id}><span>{offer.seller.username} · {offer.price.toString()} {request.currency} · {offer.deliveryInfo ?? "Teslimat belirtilmemiş"}</span><AdminStatus value={offer.status} /></p>) : <p className="admin-note">Bu talebe henüz teklif gelmedi.</p>}</section></div>; }
